#ifndef IMPORTS_H
#define IMPORTS_H


#include <linux/module.h> // Needed by all modules
#include <linux/kernel.h> // Needed for init kernel function prototypes
#include <linux/init.h>   // Needed for the macros
#include <linux/usb.h>

#endif // IMPORTS_H
